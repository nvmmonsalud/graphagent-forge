# Dev / test tooling — not required to run the app in production.
-r requirements.txt

pytest>=8.3.0
pytest-asyncio>=0.24.0
ruff>=0.6.0
streamlit>=1.38.0
# httpx is already a runtime dependency (pulled in via -r above); tests use it
# as an ASGI test transport.
